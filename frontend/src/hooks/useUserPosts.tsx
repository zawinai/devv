
import { useCT } from "./useCT";

// import { useQuery } from "react-query";
// import { getUserPosts } from "../api/data";

// export const useUserPosts = async () => {
//   const { auth, dispatch } = useCT();

//   if (auth?.accessToken) {
//     const {isLoading,isError,error,data: userPostList,} = useQuery("posts", getUserPosts(auth.username));
    
//     dispatch({ type: "setUserPosts", payload: userPostList });
//   }

//   return;
// };
